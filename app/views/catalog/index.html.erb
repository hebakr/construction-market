<div class="row">
  <% @categories.each do |category| %>
  <div class="col-md-4 mb-2">
        <%=  link_to category.name, catalog_path(category), :class => "btn btn-info btn-block" %>
  </div>
  <%  end %>
</div>
